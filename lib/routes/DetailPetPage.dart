import 'package:flutter/material.dart';

class DetailPetPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: <Widget>[
          Container(
            alignment: Alignment.center,
            child: Text('Detalle Amigo'),
          ),
        ],
      ),
    );
  }
}
